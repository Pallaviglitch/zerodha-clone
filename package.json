{
  "name": "zerodha-clone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../dashboard && npm install && cd ../frontend && npm install",
    "postinstall": "npm install --prefix frontend",
    "build": "cd frontend && npm run build",
    "start:backend": "cd backend && npm start",
    "start:dashboard": "cd dashboard && npm start",
    "start": "npx concurrently \"npm run start:backend\" \"npm run start:dashboard\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
